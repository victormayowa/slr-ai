export type ProtocolItem = { id: string; text: string; status: 'pending' | 'accepted' | 'rejected' };

export type SearchItem = { id: string; database: string; string: string; status: 'pending' | 'accepted' | 'rejected' };
